[
    {
        "description": "object schema - propertyNames",
        "schema": {"type": "object", "propertyNames": {"pattern": "^[A-Za-z_][A-Za-z0-9_]*$"}},
        "valid": true
    },
    {
        "description": "object schema - propertyNames with wrong pattern",
        "schema": {"type": "object", "propertyNames": {"pattern": "wrong //\\ patern"}},
        "valid": false
    }
]
