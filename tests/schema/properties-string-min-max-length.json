[
    {
        "description": "string schema - valid minLength",
        "schema": {"type": "string", "minLength": 1},
        "valid": true
    },
    {
        "description": "string schema - minLength with string",
        "schema": {"type": "string", "minLength": "1"},
        "valid": false
    },
    {
        "description": "string schema - wrong maxLength with string",
        "schema": {"type": "string", "maxLength": "1"},
        "valid": false
    },
    {
        "description": "string schema - valid maxLength with string",
        "schema": {"type": "string", "maxLength": 10},
        "valid": true
    },
    {
        "description": "string schema - minLength is higher than maxLength",
        "schema": {"type": "string", "minLength": 10, "maxLength": 1},
        "valid": false
    }
]
