[
    {
        "description": "wrong type structure",
        "schema": {"type": {"wrong": "structure"}},
        "valid": false
    },
    {
        "description": "integer type with string format",
        "schema": {"type": "integer", "format": "email"},
        "valid": false
    },
    {
        "description": "integer type with string property->minLength",
        "schema": {"type": "integer", "minLength": 5},
        "valid": false
    },
    {
        "description": "string type with array property->uniqueItems",
        "schema": {"type": "string", "uniqueItems": 10},
        "valid": false
    },
    {
        "description": "unknown format",
        "schema": {"format": "unknown-format-asdf"},
        "valid": false
    }
]
